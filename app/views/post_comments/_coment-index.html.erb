 <table class="table">
	<tbody>
		<% book.post_comments.each do |post_comments| %>
		<tr>
	  <td><%= link_to(post_comments.user) do %>
	    <%= image_tag post_comments.user.get_profile_image, size:"100x100" %><br>
	    <%= post_comments.user.name %>
	    <% end %>
	  </td>
			<td><%= post_comments.comment %></td>
			<% if post_comments.user_id == current_user.id %>
			<td><%= link_to 'Destroy',book_post_comment_path(book.id,post_comments.id), method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger" , remote: true %></td>
			<% end %>
		</tr>
		<% end %>
	</tbody>
 </table>